import {React, useEffect} from 'react';
import { View, StyleSheet, Text, TouchableOpacity, Alert } from 'react-native';

export default UserPlan = ({ navigation }) => {

    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({

});